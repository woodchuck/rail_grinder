## Install

You will need cmake installed to compile the rugged gem's included version of libgit2.

    $ git clone ...
    $ cd rail_grinder
    $ bundle install

## Todo

 * Make into a gem
 * put rg on the PATH
 * specs!!
 * Example docs
